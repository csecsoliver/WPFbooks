<!-- ReSharper disable once ObsoleteElementError -->
<Window x:Class="WPFbooks.GenreManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WPFbooks"
        mc:Ignorable="d"
        Title="Műfaj Kezelő" Height="500" Width="700" FontSize="14"
        ThemeMode="Dark"
        Loaded="Window_Loaded">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Content="Műfajok Kezelése" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Genre List -->
            <GroupBox Grid.Column="0" Header="Műfajok" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <ListBox Name="GenreListBox" Grid.Row="0" SelectionChanged="GenreListBox_SelectionChanged" Margin="5">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding BookCount}" FontSize="11" Foreground="Gray"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    
                    <Button Name="btnNewGenre" Grid.Row="1" Content="+ Új Műfaj" Margin="5" Padding="10,5" Click="btnNewGenre_Click"/>
                </Grid>
            </GroupBox>
            
            <!-- Genre Details -->
            <GroupBox Grid.Column="1" Header="Részletek" Margin="10,0,0,0">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <Label Grid.Row="0" Content="Műfaj neve:" FontWeight="Bold"/>
                    <TextBox Name="txtGenreName" Grid.Row="1" Margin="0,0,0,10" Padding="5"/>
                    
                    <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,10,0,0">
                        <TextBlock Name="lblBookCount" Text="Könyvek száma: 0" FontStyle="Italic" Foreground="Gray"/>
                        <TextBlock Name="lblErrorMessage" Text="" Foreground="Red" Visibility="Collapsed" Margin="0,10,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Name="btnSave" Content="Mentés" Width="80" Margin="0,0,10,0" Click="btnSave_Click"/>
                        <Button Name="btnDelete" Content="Törlés" Width="80" Click="btnDelete_Click"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </Grid>
        
        <Button Grid.Row="2" Name="btnClose" Content="Bezárás" Width="100" HorizontalAlignment="Right" Margin="0,10,0,0" Click="btnClose_Click"/>
    </Grid>
</Window>

